ktor {
    deployment {
        port = 8080
        watch = [ kakaopay.ppurigi ]
    }

    application {
        modules = [ ServerKt.main ]
    }
}

maria {
    dataSourceClassName = com.zaxxer.hikari.HikariDataSource
    dataSource {
      user = sample
      password = sample
      password = ${?PASSWORD}
      url = "jdbc:mariadb://data-maria:3306/ppurigi"
      url = ${?URL}
    }
}

h2 {
    driverClassName = "org.h2.Driver"
    dataSource {
      user = "sa"
      password = ""
      url = "jdbc:h2:mem:test;MV_STORE=FALSE"
    }
}

db_type = h2
db_type = ${?DB_TYPE}
